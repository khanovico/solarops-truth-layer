interface:
  display_name: "Run Tests Workflow"
  short_description: "Run JobCRM tests in the right order"
  default_prompt: "Use $run-tests-workflow to validate this JobCRM change with the right test commands."
