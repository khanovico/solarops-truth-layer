
# TDD / coverage

- When practical: failing test → minimal pass → refactor (green).
- New behavior/fixes: tests for happy, failure, edge cases; regression tests for bugs. Ship only when tests pass.
- Prefer focused, deterministic tests; unit + integration + E2E by risk. Note gaps + follow-up if full coverage not in one change.
